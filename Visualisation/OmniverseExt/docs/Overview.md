# Overview

`EvoSimViz` (`evosim.viz`) is a minimal Omniverse Python UI extension.

It currently provides a small window with placeholder controls for an evolution simulation.
The simulation logic will be integrated in a subsequent step.
